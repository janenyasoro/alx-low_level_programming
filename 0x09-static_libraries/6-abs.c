#include "main.h"
/**
 * _abs - prints the absolute value of a number
 *@i: number targeted
 *Return: returns 0
 */
int _abs(int i)
{
	if (a > 0)
	{
		return (a);
	}
	else if (a < 0)
	{
		a = a * -1;
		return (a);
	}
	else
	{
		return (a);
	}
}
